import { Game } from '0g/dist-esm/Game';
import { Query, QueryComponentFilter } from '0g/dist-esm/Query';
export declare class QueryManager {
    private game;
    private pool;
    constructor(game: Game);
    create<Def extends QueryComponentFilter>(userDef: Def): Query<Def>;
    release(query: Query<any>): void;
}
